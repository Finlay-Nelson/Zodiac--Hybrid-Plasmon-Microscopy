"""
control.py

Handles low-level hardware control such as cameras, motors, and light sources.
Replace placeholder functions with real hardware API calls.
"""

def initialize_camera():
    """Initialize the microscope camera hardware."""
    print("[Placeholder] Camera initialized")

def capture_image(filename="image.png"):
    """Capture an image and save it to the given filename."""
    print(f"[Placeholder] Capturing image -> {filename}")
